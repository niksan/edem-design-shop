.name= link_to product.name, category_product_path(product.category, product)
= link_to category_product_path(product.category, product) do
  = image_tag product.main_photo.small
.price= number_to_currency(product.price).gsub('.00', '')
= link_to 'в корзину', cart_items_path(product), class: 'btn btn-info btn-xs', remote: true, method: :post
